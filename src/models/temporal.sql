-- TODO: Define the temporal model with (id, period) as primary key [2]
CREATE TABLE temporal_model (
    
);

-- TODO: Add the insert(id, period) procedure [3]

-- TODO: Add the delete(id) procedure [4]

-- TODO: Add the modify(id, period) procedure [5]

-- TODO: Add temporal projection, selection, union, set difference, and join functions (needs coalesce function first) [9]